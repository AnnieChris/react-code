import React from 'react';

function Child2({selectedMessage}) {
    return (
        <div>
            
            <h2>{selectedMessage}</h2> 
            {/* this displays the message from child */} 

        </div>
    );
}

export default Child2;